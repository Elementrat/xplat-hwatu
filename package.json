{
  "name": "food-xplat",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/config/*",
    "packages/shared/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "engines": {
    "node": ">=18.17.1"
  },
  "packageManager": "yarn@1.22.17",
  "devDependencies": {
    "turborepo": "^0.0.1"
  }
}
